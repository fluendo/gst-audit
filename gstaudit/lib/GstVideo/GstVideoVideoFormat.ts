export namespace GstVideoVideoFormat {
  export const UNKNOWN: 'unknown' = 'unknown';
  export const ENCODED: 'encoded' = 'encoded';
  export const I420: 'i420' = 'i420';
  export const YV12: 'yv12' = 'yv12';
  export const YUY2: 'yuy2' = 'yuy2';
  export const UYVY: 'uyvy' = 'uyvy';
  export const AYUV: 'ayuv' = 'ayuv';
  export const RGBX: 'rgbx' = 'rgbx';
  export const BGRX: 'bgrx' = 'bgrx';
  export const XRGB: 'xrgb' = 'xrgb';
  export const XBGR: 'xbgr' = 'xbgr';
  export const RGBA: 'rgba' = 'rgba';
  export const BGRA: 'bgra' = 'bgra';
  export const ARGB: 'argb' = 'argb';
  export const ABGR: 'abgr' = 'abgr';
  export const RGB: 'rgb' = 'rgb';
  export const BGR: 'bgr' = 'bgr';
  export const Y41B: 'y41b' = 'y41b';
  export const Y42B: 'y42b' = 'y42b';
  export const YVYU: 'yvyu' = 'yvyu';
  export const Y444: 'y444' = 'y444';
  export const V210: 'v210' = 'v210';
  export const V216: 'v216' = 'v216';
  export const NV12: 'nv12' = 'nv12';
  export const NV21: 'nv21' = 'nv21';
  export const GRAY8: 'gray8' = 'gray8';
  export const GRAY16_BE: 'gray16_be' = 'gray16_be';
  export const GRAY16_LE: 'gray16_le' = 'gray16_le';
  export const V308: 'v308' = 'v308';
  export const RGB16: 'rgb16' = 'rgb16';
  export const BGR16: 'bgr16' = 'bgr16';
  export const RGB15: 'rgb15' = 'rgb15';
  export const BGR15: 'bgr15' = 'bgr15';
  export const UYVP: 'uyvp' = 'uyvp';
  export const A420: 'a420' = 'a420';
  export const RGB8P: 'rgb8p' = 'rgb8p';
  export const YUV9: 'yuv9' = 'yuv9';
  export const YVU9: 'yvu9' = 'yvu9';
  export const IYU1: 'iyu1' = 'iyu1';
  export const ARGB64: 'argb64' = 'argb64';
  export const AYUV64: 'ayuv64' = 'ayuv64';
  export const R210: 'r210' = 'r210';
  export const I420_10BE: 'i420_10be' = 'i420_10be';
  export const I420_10LE: 'i420_10le' = 'i420_10le';
  export const I422_10BE: 'i422_10be' = 'i422_10be';
  export const I422_10LE: 'i422_10le' = 'i422_10le';
  export const Y444_10BE: 'y444_10be' = 'y444_10be';
  export const Y444_10LE: 'y444_10le' = 'y444_10le';
  export const GBR: 'gbr' = 'gbr';
  export const GBR_10BE: 'gbr_10be' = 'gbr_10be';
  export const GBR_10LE: 'gbr_10le' = 'gbr_10le';
  export const NV16: 'nv16' = 'nv16';
  export const NV24: 'nv24' = 'nv24';
  export const NV12_64Z32: 'nv12_64z32' = 'nv12_64z32';
  export const A420_10BE: 'a420_10be' = 'a420_10be';
  export const A420_10LE: 'a420_10le' = 'a420_10le';
  export const A422_10BE: 'a422_10be' = 'a422_10be';
  export const A422_10LE: 'a422_10le' = 'a422_10le';
  export const A444_10BE: 'a444_10be' = 'a444_10be';
  export const A444_10LE: 'a444_10le' = 'a444_10le';
  export const NV61: 'nv61' = 'nv61';
  export const P010_10BE: 'p010_10be' = 'p010_10be';
  export const P010_10LE: 'p010_10le' = 'p010_10le';
  export const IYU2: 'iyu2' = 'iyu2';
  export const VYUY: 'vyuy' = 'vyuy';
  export const GBRA: 'gbra' = 'gbra';
  export const GBRA_10BE: 'gbra_10be' = 'gbra_10be';
  export const GBRA_10LE: 'gbra_10le' = 'gbra_10le';
  export const GBR_12BE: 'gbr_12be' = 'gbr_12be';
  export const GBR_12LE: 'gbr_12le' = 'gbr_12le';
  export const GBRA_12BE: 'gbra_12be' = 'gbra_12be';
  export const GBRA_12LE: 'gbra_12le' = 'gbra_12le';
  export const I420_12BE: 'i420_12be' = 'i420_12be';
  export const I420_12LE: 'i420_12le' = 'i420_12le';
  export const I422_12BE: 'i422_12be' = 'i422_12be';
  export const I422_12LE: 'i422_12le' = 'i422_12le';
  export const Y444_12BE: 'y444_12be' = 'y444_12be';
  export const Y444_12LE: 'y444_12le' = 'y444_12le';
  export const GRAY10_LE32: 'gray10_le32' = 'gray10_le32';
  export const NV12_10LE32: 'nv12_10le32' = 'nv12_10le32';
  export const NV16_10LE32: 'nv16_10le32' = 'nv16_10le32';
  export const NV12_10LE40: 'nv12_10le40' = 'nv12_10le40';
  export const Y210: 'y210' = 'y210';
  export const Y410: 'y410' = 'y410';
  export const VUYA: 'vuya' = 'vuya';
  export const BGR10A2_LE: 'bgr10a2_le' = 'bgr10a2_le';
  export const RGB10A2_LE: 'rgb10a2_le' = 'rgb10a2_le';
  export const Y444_16BE: 'y444_16be' = 'y444_16be';
  export const Y444_16LE: 'y444_16le' = 'y444_16le';
  export const P016_BE: 'p016_be' = 'p016_be';
  export const P016_LE: 'p016_le' = 'p016_le';
  export const P012_BE: 'p012_be' = 'p012_be';
  export const P012_LE: 'p012_le' = 'p012_le';
  export const Y212_BE: 'y212_be' = 'y212_be';
  export const Y212_LE: 'y212_le' = 'y212_le';
  export const Y412_BE: 'y412_be' = 'y412_be';
  export const Y412_LE: 'y412_le' = 'y412_le';
  export const NV12_4L4: 'nv12_4l4' = 'nv12_4l4';
  export const NV12_32L32: 'nv12_32l32' = 'nv12_32l32';
  export const RGBP: 'rgbp' = 'rgbp';
  export const BGRP: 'bgrp' = 'bgrp';
  export const AV12: 'av12' = 'av12';
  export const ARGB64_LE: 'argb64_le' = 'argb64_le';
  export const ARGB64_BE: 'argb64_be' = 'argb64_be';
  export const RGBA64_LE: 'rgba64_le' = 'rgba64_le';
  export const RGBA64_BE: 'rgba64_be' = 'rgba64_be';
  export const BGRA64_LE: 'bgra64_le' = 'bgra64_le';
  export const BGRA64_BE: 'bgra64_be' = 'bgra64_be';
  export const ABGR64_LE: 'abgr64_le' = 'abgr64_le';
  export const ABGR64_BE: 'abgr64_be' = 'abgr64_be';
  export const NV12_16L32S: 'nv12_16l32s' = 'nv12_16l32s';
  export const NV12_8L128: 'nv12_8l128' = 'nv12_8l128';
  export const NV12_10BE_8L128: 'nv12_10be_8l128' = 'nv12_10be_8l128';
  export const NV12_10LE40_4L4: 'nv12_10le40_4l4' = 'nv12_10le40_4l4';
  export const DMA_DRM: 'dma_drm' = 'dma_drm';
  export const MT2110T: 'mt2110t' = 'mt2110t';
  export const MT2110R: 'mt2110r' = 'mt2110r';
  export const A422: 'a422' = 'a422';
  export const A444: 'a444' = 'a444';
  export const A444_12LE: 'a444_12le' = 'a444_12le';
  export const A444_12BE: 'a444_12be' = 'a444_12be';
  export const A422_12LE: 'a422_12le' = 'a422_12le';
  export const A422_12BE: 'a422_12be' = 'a422_12be';
  export const A420_12LE: 'a420_12le' = 'a420_12le';
  export const A420_12BE: 'a420_12be' = 'a420_12be';
  export const A444_16LE: 'a444_16le' = 'a444_16le';
  export const A444_16BE: 'a444_16be' = 'a444_16be';
  export const A422_16LE: 'a422_16le' = 'a422_16le';
  export const A422_16BE: 'a422_16be' = 'a422_16be';
  export const A420_16LE: 'a420_16le' = 'a420_16le';
  export const A420_16BE: 'a420_16be' = 'a420_16be';
  export const GBR_16LE: 'gbr_16le' = 'gbr_16le';
  export const GBR_16BE: 'gbr_16be' = 'gbr_16be';
  export const RBGA: 'rbga' = 'rbga';
    







 
  export async function from_fourcc(fourcc: number): Promise<GstVideoVideoFormatValue> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/from_fourcc`, apiConfig.baseUrl);
    // Primitive parameter
    url.searchParams.append('fourcc', String(fourcc));
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
    return data.return;


    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

    







 
  export async function from_masks(depth: number, bpp: number, endianness: number, red_mask: number, green_mask: number, blue_mask: number, alpha_mask: number): Promise<GstVideoVideoFormatValue> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/from_masks`, apiConfig.baseUrl);
    // Primitive parameter
    url.searchParams.append('depth', String(depth));
    // Primitive parameter
    url.searchParams.append('bpp', String(bpp));
    // Primitive parameter
    url.searchParams.append('endianness', String(endianness));
    // Primitive parameter
    url.searchParams.append('red_mask', String(red_mask));
    // Primitive parameter
    url.searchParams.append('green_mask', String(green_mask));
    // Primitive parameter
    url.searchParams.append('blue_mask', String(blue_mask));
    // Primitive parameter
    url.searchParams.append('alpha_mask', String(alpha_mask));
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
    return data.return;


    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

    







 
  export async function from_string(format: string): Promise<GstVideoVideoFormatValue> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/from_string`, apiConfig.baseUrl);
    // Primitive parameter
    url.searchParams.append('format', String(format));
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
    return data.return;


    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

    







 
  export async function get_info(format: GstVideoVideoFormatValue): Promise<GstVideoVideoFormatInfo> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/get_info`, apiConfig.baseUrl);
    // Primitive parameter
    url.searchParams.append('format', String(format));
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
    // Return value is a struct, instantiate it from the ptr
if (data.return && typeof data.return === 'object' && 'ptr' in data.return) {
const instance = await GstVideoVideoFormatInfo.create(data.return.ptr, 'none');
  return instance;
}
return Promise.reject("Call failed");


    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

    







 
  export async function get_palette(format: GstVideoVideoFormatValue): Promise<{size: number, return: Pointer | null}> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/get_palette`, apiConfig.baseUrl);
    // Primitive parameter
    url.searchParams.append('format', String(format));
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
        const result: any = {};
      // Handle return parameter: size
      result.size = await (async () => {
        return data.size;

      })();
      // Handle return parameter: return
      result.return = await (async () => {
        return data.return;

      })();
      return result;

    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

    







 
  export async function to_fourcc(format: GstVideoVideoFormatValue): Promise<number> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/to_fourcc`, apiConfig.baseUrl);
    // Primitive parameter
    url.searchParams.append('format', String(format));
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
    return data.return;


    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

    







 
  export async function to_string(format: GstVideoVideoFormatValue): Promise<string> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/to_string`, apiConfig.baseUrl);
    // Primitive parameter
    url.searchParams.append('format', String(format));
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
    return data.return;


    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

    







 
  export async function get_type(): Promise<string> {
    // Increment ref for parameters with full transfer ownership
    const url = new URL(`${apiConfig.normalizedBasePath}/GstVideo/VideoFormat/get_type`, apiConfig.baseUrl);
    try {
      // Auto-inject correlation ID if we're in a callback context
      const correlationId = getActiveCorrelationId();
      const headers: Record<string, string> = {};
      if (correlationId) {
        headers['X-Correlation-Id'] = correlationId;
      }
      const response = await fetch(url.toString(), Object.keys(headers).length > 0 ? { headers } : undefined);
      if (!response.ok) {
        // If the call fails, unref the objects we ref'd
        throw new Error(`HTTP error! status: ${response.status}`);
      }
      const data = await response.json();
    return data.return;


    } catch (error) {
      // If there's an error, unref the objects we ref'd
      throw error;
    }
  }

  }
  export type GstVideoVideoFormatValue = "unknown" | "encoded" | "i420" | "yv12" | "yuy2" | "uyvy" | "ayuv" | "rgbx" | "bgrx" | "xrgb" | "xbgr" | "rgba" | "bgra" | "argb" | "abgr" | "rgb" | "bgr" | "y41b" | "y42b" | "yvyu" | "y444" | "v210" | "v216" | "nv12" | "nv21" | "gray8" | "gray16_be" | "gray16_le" | "v308" | "rgb16" | "bgr16" | "rgb15" | "bgr15" | "uyvp" | "a420" | "rgb8p" | "yuv9" | "yvu9" | "iyu1" | "argb64" | "ayuv64" | "r210" | "i420_10be" | "i420_10le" | "i422_10be" | "i422_10le" | "y444_10be" | "y444_10le" | "gbr" | "gbr_10be" | "gbr_10le" | "nv16" | "nv24" | "nv12_64z32" | "a420_10be" | "a420_10le" | "a422_10be" | "a422_10le" | "a444_10be" | "a444_10le" | "nv61" | "p010_10be" | "p010_10le" | "iyu2" | "vyuy" | "gbra" | "gbra_10be" | "gbra_10le" | "gbr_12be" | "gbr_12le" | "gbra_12be" | "gbra_12le" | "i420_12be" | "i420_12le" | "i422_12be" | "i422_12le" | "y444_12be" | "y444_12le" | "gray10_le32" | "nv12_10le32" | "nv16_10le32" | "nv12_10le40" | "y210" | "y410" | "vuya" | "bgr10a2_le" | "rgb10a2_le" | "y444_16be" | "y444_16le" | "p016_be" | "p016_le" | "p012_be" | "p012_le" | "y212_be" | "y212_le" | "y412_be" | "y412_le" | "nv12_4l4" | "nv12_32l32" | "rgbp" | "bgrp" | "av12" | "argb64_le" | "argb64_be" | "rgba64_le" | "rgba64_be" | "bgra64_le" | "bgra64_be" | "abgr64_le" | "abgr64_be" | "nv12_16l32s" | "nv12_8l128" | "nv12_10be_8l128" | "nv12_10le40_4l4" | "dma_drm" | "mt2110t" | "mt2110r" | "a422" | "a444" | "a444_12le" | "a444_12be" | "a422_12le" | "a422_12be" | "a420_12le" | "a420_12be" | "a444_16le" | "a444_16be" | "a422_16le" | "a422_16be" | "a420_16le" | "a420_16be" | "gbr_16le" | "gbr_16be" | "rbga";
