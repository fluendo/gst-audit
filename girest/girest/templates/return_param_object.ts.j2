      // Return value is an object, instantiate it from the ptr
      if (data.{{ return_param.name }} && typeof data.{{ return_param.name }} === 'object' && 'ptr' in data.{{ return_param.name }}) {
{% if return_param.can_be_null %}
        if (data.{{ return_param.name }}.ptr === null) {
          return null;
        }
{% endif %}
        // Use static factory method to properly await ref counting
        {% if return_param.parent.parent.parent._is_child_class(return_param.type.ref_schema.name) -%}
        // Dynamic import to avoid circular dependency with child class
        const { {{ return_param.type.ref_schema.valid_name }} } = await import('./{{ return_param.type.ref_schema.name }}');
        {% endif -%}
        const instance = await {{ return_param.type.ref_schema.valid_name }}.create(data.{{ return_param.name }}.ptr, '{{ return_param.transfer  }}');
        return instance;
      }
      return Promise.reject("Call failed");

